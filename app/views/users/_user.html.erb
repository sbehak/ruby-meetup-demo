<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Profile picture:</strong>
    <%= link_to user.profile_picture.filename, user.profile_picture if user.profile_picture.attached? %>
  </p>

</div>
